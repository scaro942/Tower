[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA 폴백: 모든 경로를 index.html로 (클라이언트 렌더)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
